**新人大陆期货实盘最匹配完整技术栈（2026 年 2 月版）**  
专为**零基础 → 先期货实盘**设计，后期无缝加 OpenBB。整个栈极简、成熟、社区最活跃。

| 层级     | 工具                          | 作用                              | 为什么选它（新人视角）                  |
|----------|-------------------------------|-----------------------------------|-----------------------------------------|
| **安装平台** | **VeighNa Studio 4.3.0**     | 一键安装所有环境 + 图形界面      | 最傻瓜！双击 exe 就完事，无需手动配 Python |
| **核心框架** | VeighNa (vn.py) 4.3.0        | 策略运行 + 实盘执行 + 风控       | 大陆期货 CTP 最强，几乎所有私募都在用   |
| **实盘网关** | CTP Gateway（内置）          | 直接连期货公司下单                | tick 级实时、自动换月、主连合约全支持   |
| **数据层**（初期） | vnpy_tushare + vnpy_akshare | 免费历史/实时期货数据            | 够用、不用翻墙、积分免费够日常         |
| **策略开发** | CTA Strategy（图形化）       | 趋势/日内/套利策略                | 有现成模板 + 参数优化 + 回测界面       |
| **监控**     | VeighNa Station + 微信/钉钉  | 实时仓位、日志、告警              | 内置，支持手机看盘                      |
| **后期扩展** | OpenBB + openbb-akshare      | 统一数据 + AI 信号                | 直接喂给 vn.py（示例代码在下面）       |

---

### 一键安装命令 + 步骤（Windows 电脑推荐，香港 VPS 也一样）

1. **下载 VeighNa Studio（最重要！）**  
   直接点这个链接下载最新版（2025.12.24 发布 4.3.0，已带 AI 助手）：  
   **https://download.vnpy.com/veighna_studio-4.3.0.exe**

2. **双击运行安装**（全程下一步即可，10-15 分钟）  
   安装完桌面会出现 **VeighNa Station** 图标，双击打开。

3. **安装数据插件**（Studio 里打开终端，复制粘贴运行）：
   ```bash
   pip install vnpy_tushare vnpy_akshare
   ```

4. **重启 VeighNa Station** → 左侧「全局配置」→「数据服务」里就能看到 Tushare / AKShare。

---

### 先用 SimNow 仿真练手（强烈建议！实盘前跑 1-2 周）

1. 注册 SimNow（中金所官方仿真）：http://www.simnow.com.cn/  
   用手机号注册 → 下载「SimNow 客户端」测试行情。

2. 在 VeighNa Station 里添加 CTP 连接：  
   - 网关 → 添加 → CTP  
   - BrokerID：**9999**  
   - 行情服务器：**180.168.146.187:10131**（或官网最新）  
   - 交易服务器：**180.168.146.187:10130**  
   - 用户名/密码：SimNow 给你的  
   - 产品名称：**simnow_client_test**  
   - 授权码：**0000000000000000**

3. 连接成功后 → 加载 CTA 策略模板 → 选 IF 或 RB 等品种 → 直接点「启动」跑仿真。

---

### 实盘开户（香港用户可远程办）

支持香港居民远程开户的期货公司（2026 年常见）：
- **南华期货**（Nanhua）—— 最友好，有香港分公司，官网有「海外客户开户」通道，用 HKID + 地址证明即可。
- **永安期货**、**徽商期货**、**中信期货** 等也支持。

流程：  
官网/微信搜“期货公司名 + 香港开户” → 客服发资料表 → 视频认证 → 2-5 个工作日开户成功 → 他们给你 CTP 地址、BrokerID、账号密码。

**注意**：香港 VPS（阿里云香港 或 腾讯云香港，推荐 2核4G 每月 ~80 元）跑 24h，延迟 <50ms。

---

### 简单策略示例（双均线，期货专用）

在 VeighNa Station 里「策略管理」→ 新建策略 → 复制下面代码保存为 `MyFuturesMA.py`：

```python
from vnpy_ctastrategy import CtaTemplate
from vnpy.trader.object import BarData
import talib
import numpy as np

class MyFuturesMA(CtaTemplate):
    """新人期货双均线策略"""
    author = "新人"
    
    fast_window = 10      # 快线周期
    slow_window = 30      # 慢线周期
    fixed_size = 1        # 每次下单手数（根据资金调整）
    
    parameters = ["fast_window", "slow_window", "fixed_size"]
    variables = ["pos", "fast_ma", "slow_ma"]

    def on_init(self):
        self.write_log("策略初始化")
        self.load_bar(100)   # 加载历史 100 根 K 线

    def on_bar(self, bar: BarData):
        """每根 K 线触发"""
        self.cancel_all()   # 先撤单
        
        # 计算均线
        self.fast_ma = self.get_ma(self.fast_window)
        self.slow_ma = self.get_ma(self.slow_window)
        
        if self.pos == 0:                    # 空仓
            if self.fast_ma > self.slow_ma:  # 金叉 → 买开
                self.buy(bar.close_price * 1.005, self.fixed_size)
        elif self.pos > 0:                   # 多仓
            if self.fast_ma < self.slow_ma:  # 死叉 → 平多
                self.sell(bar.close_price * 0.995, abs(self.pos))
        
        self.put_event()

    def get_ma(self, period: int):
        """获取均线（简化版）"""
        # 实际用 self.get_bar_data() 或缓存，这里简化示例
        # 真实策略建议用 talib.MA
        pass   # 完整代码可在社区模板复制
```

（完整带 talib 版可在 VeighNa 论坛搜「双均线期货」复制）

---

### 后期加 OpenBB 数据喂 vn.py（无缝升级）

等你熟悉后，只需加 3 行代码：

```bash
pip install openbb openbb-akshare
```

在策略里加（示例：每分钟用 OpenBB/AKShare 拿最新数据）：

```python
from openbb import obb
import akshare as ak   # 或直接 openbb

# 在 on_timer 或单独线程里
def get_latest_bar(self):
    # AKShare 期货主连（OpenBB 也能调）
    df = ak.futures_main_sina(symbol="RB0")   # 螺纹钢主连
    # 或用 OpenBB（未来支持更全）
    # price = obb.derivatives.futures.price(symbol="RB0", provider="akshare")
    
    bar = BarData(...)   # 转成 vn.py 的 BarData 对象
    self.on_bar(bar)     # 喂给策略
```

这样 **OpenBB 负责高级数据 + AI 信号**，**vn.py 只管下单执行**，完美解耦。
