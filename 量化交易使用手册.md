# 量化交易使用手册

> 适用平台：macOS（Apple Silicon / Intel）
> 覆盖市场：港股 + 大陆期货
> 环境路径：`~/vnpy_env`（Python 3.11）

---

## 快速开始

每次使用前激活环境：

```bash
source ~/vnpy_env/bin/activate
```

验证安装：

```bash
python -c "import vnpy; import futu; import backtrader; import akshare; print('环境正常')"
```

---

## 第一部分：港股量化

### 1. 环境准备

富途 OpenAPI 需要本地运行 FutuOpenD 网关进程：

1. 下载 FutuOpenD：[https://www.futunn.com/download/openAPI](https://www.futunn.com/download/openAPI)
2. 安装后双击启动，登录富途牛牛账号
3. 默认监听 `127.0.0.1:11111`

开户说明：
- 香港居民：HKID + 地址证明，线上开户 1-3 个工作日
- 开户后自动获得仿真账户，无需入金即可测试 API
- 港股佣金：终身 0 佣金

---

### 2. 获取港股数据

#### AKShare（免费历史数据）

```python
import akshare as ak

# 港股历史日线（前复权）
df = ak.stock_hk_hist(
    symbol="00700",          # 腾讯
    period="daily",
    start_date="20230101",
    end_date="20241231",
    adjust="qfq"             # qfq=前复权, hfq=后复权, 空=不复权
)
print(df.tail())

# 港股实时行情快照
df_rt = ak.stock_hk_spot_em()
print(df_rt[df_rt['代码'] == '00700'])

# 港股分钟线
df_min = ak.stock_hk_hist_min_em(
    symbol="00700",
    period="60",             # 60分钟线，可选 1/5/15/30/60
    start_date="2024-01-01 09:30:00",
    end_date="2024-12-31 16:00:00",
    adjust="qfq"
)
```

常用港股代码：

| 代码 | 股票 | 代码 | 股票 |
|------|------|------|------|
| 00700 | 腾讯 | 09988 | 阿里巴巴 |
| 03690 | 美团 | 00941 | 中国移动 |
| 00005 | 汇丰 | 02318 | 中国平安 |
| 02800 | 盈富基金（恒指ETF） | 00388 | 港交所 |

#### Futu OpenAPI（实时行情）

```python
import futu as ft

quote_ctx = ft.OpenQuoteContext(host='127.0.0.1', port=11111)

# 实时报价
ret, data = quote_ctx.get_market_snapshot(['HK.00700', 'HK.09988'])
if ret == ft.RET_OK:
    print(data[['code', 'last_price', 'volume', 'turnover']])

# 历史 K 线（Futu 提供，质量更高）
ret, data, _ = quote_ctx.request_history_kline(
    'HK.00700',
    start='2023-01-01',
    end='2024-12-31',
    ktype=ft.KLType.K_DAY,
    autype=ft.AuType.QFQ
)
print(data)

quote_ctx.close()
```

---

### 3. 港股回测（Backtrader）

```python
import backtrader as bt
import akshare as ak
import pandas as pd

def get_hk_data(symbol, start, end):
    """从 AKShare 获取港股数据并转换为 Backtrader 格式"""
    df = ak.stock_hk_hist(symbol=symbol, period="daily",
                          start_date=start, end_date=end, adjust="qfq")
    df.columns = ['date', 'open', 'close', 'high', 'low',
                  'volume', 'turnover', 'amplitude', 'pct_change',
                  'change', 'turnover_rate']
    df['date'] = pd.to_datetime(df['date'])
    df = df.set_index('date')
    df['openinterest'] = 0
    return df[['open', 'high', 'low', 'close', 'volume', 'openinterest']]


class DoubleMaStrategy(bt.Strategy):
    params = (('fast', 10), ('slow', 30),)

    def __init__(self):
        self.fast_ma = bt.indicators.SMA(period=self.p.fast)
        self.slow_ma = bt.indicators.SMA(period=self.p.slow)
        self.crossover = bt.indicators.CrossOver(self.fast_ma, self.slow_ma)

    def next(self):
        if not self.position:
            if self.crossover > 0:    # 金叉买入
                self.buy()
        elif self.crossover < 0:      # 死叉卖出
            self.close()


# 运行回测
cerebro = bt.Cerebro()
cerebro.addstrategy(DoubleMaStrategy, fast=10, slow=30)

df = get_hk_data("00700", "20230101", "20241231")
cerebro.adddata(bt.feeds.PandasData(dataname=df))

cerebro.broker.setcash(100000)         # 初始资金 10 万港币
cerebro.broker.setcommission(0.001)    # 手续费 0.1%（含印花税）
cerebro.addsizer(bt.sizers.PercentSizer, percents=95)

cerebro.addanalyzer(bt.analyzers.SharpeRatio, _name='sharpe')
cerebro.addanalyzer(bt.analyzers.DrawDown, _name='drawdown')
cerebro.addanalyzer(bt.analyzers.Returns, _name='returns')

results = cerebro.run()
strat = results[0]

print(f"最终资金: {cerebro.broker.getvalue():.2f}")
print(f"夏普比率: {strat.analyzers.sharpe.get_analysis()['sharperatio']:.2f}")
print(f"最大回撤: {strat.analyzers.drawdown.get_analysis()['max']['drawdown']:.2f}%")
print(f"年化收益: {strat.analyzers.returns.get_analysis()['rnorm100']:.2f}%")

cerebro.plot()
```

参数优化：

```python
cerebro = bt.Cerebro(optreturn=False)
cerebro.optstrategy(DoubleMaStrategy,
                    fast=range(5, 20, 5),
                    slow=range(20, 60, 10))
cerebro.adddata(bt.feeds.PandasData(dataname=df))
cerebro.broker.setcash(100000)
cerebro.broker.setcommission(0.001)

results = cerebro.run(maxcpus=1)
best = max(results, key=lambda x: x[0].analyzers.returns.get_analysis()['rnorm100'])
print(f"最优参数: fast={best[0].p.fast}, slow={best[0].p.slow}")
```

---

### 4. 港股实盘下单（Futu OpenAPI）

```python
import futu as ft

trd_ctx = ft.OpenSecTradeContext(
    filter_trdmarket=ft.TrdMarket.HK,
    host='127.0.0.1',
    port=11111,
    security_firm=ft.SecurityFirm.FUTUSECURITIES
)

# 查询账户资产
ret, data = trd_ctx.accinfo_query()
if ret == ft.RET_OK:
    print(data[['cash', 'total_assets', 'market_val']])

# 查询持仓
ret, data = trd_ctx.position_list_query()
print(data)

# 下单（限价单）
ret, data = trd_ctx.place_order(
    price=350.0,
    qty=100,
    code='HK.00700',
    trd_side=ft.TrdSide.BUY,
    order_type=ft.OrderType.NORMAL,
    trd_env=ft.TrdEnv.SIMULATE    # 改为 ft.TrdEnv.REAL 切换实盘
)
if ret == ft.RET_OK:
    print(f"下单成功，订单ID: {data['order_id'][0]}")

trd_ctx.close()
```

---

### 5. 港股交易规则

| 规则 | 说明 |
|------|------|
| 交易时间 | 09:30-12:00，13:00-16:00 |
| 最小单位 | 每只股票不同（1手=100/200/500股等） |
| 印花税 | 买卖双向各 0.1% |
| 结算 | T+2 交收 |
| 卖空 | 需融券，普通账户不能裸卖空 |
| 涨跌停 | 无限制 |

---

## 第二部分：大陆期货量化

> macOS 只能做策略开发和回测，CTP 实盘需要 Linux VPS 或 Windows。

### 1. 获取期货数据（AKShare）

```python
import akshare as ak

# 期货主连日线
df = ak.futures_main_sina(symbol="RB0")   # 螺纹钢主连
print(df.tail())

# 常用品种代码
# RB0 螺纹钢   I0  铁矿石   CU0 铜    AU0 黄金
# IF0 沪深300  IC0 中证500  IM0 中证1000
```

---

### 2. 期货策略开发（VeighNa CTA）

保存为 `strategy/ma_futures.py`：

```python
from vnpy_ctastrategy import CtaTemplate
from vnpy.trader.object import BarData
from vnpy.trader.utility import ArrayManager

class DoubleMaFutures(CtaTemplate):
    """双均线期货策略"""
    author = "quant"

    fast_window = 10
    slow_window = 30
    fixed_size = 1           # 每次下单手数

    parameters = ["fast_window", "slow_window", "fixed_size"]
    variables = []

    def on_init(self):
        self.am = ArrayManager(size=100)
        self.load_bar(100)

    def on_start(self):
        self.write_log("策略启动")

    def on_stop(self):
        self.write_log("策略停止")

    def on_bar(self, bar: BarData):
        self.cancel_all()
        am = self.am
        am.update_bar(bar)
        if not am.inited:
            return

        fast_ma = am.sma(self.fast_window)
        slow_ma = am.sma(self.slow_window)

        if self.pos == 0:
            if fast_ma > slow_ma:
                self.buy(bar.close_price * 1.005, self.fixed_size)
        elif self.pos > 0:
            if fast_ma < slow_ma:
                self.sell(bar.close_price * 0.995, abs(self.pos))

        self.put_event()
```

---

### 3. 期货回测（VeighNa）

```python
from vnpy_ctastrategy.backtesting import BacktestingEngine
from vnpy.trader.constant import Interval
from datetime import datetime
from strategy.ma_futures import DoubleMaFutures

engine = BacktestingEngine()
engine.set_parameters(
    vt_symbol="RB2501.SHFE",   # 螺纹钢合约
    interval=Interval.DAILY,
    start=datetime(2023, 1, 1),
    end=datetime(2024, 12, 31),
    rate=0.0003,               # 手续费率
    slippage=1,                # 滑点（元/手）
    size=10,                   # 合约乘数
    pricetick=1,               # 最小变动价位
    capital=100000,            # 初始资金
)

engine.add_strategy(DoubleMaFutures, {"fast_window": 10, "slow_window": 30})
engine.load_data()
engine.run_backtesting()
engine.calculate_result()
engine.calculate_statistics()
engine.show_chart()
```

---

### 4. SimNow 仿真实盘（Linux VPS）

在 Linux VPS 上安装：

```bash
pip install vnpy vnpy_ctp vnpy_ctastrategy vnpy_akshare
```

SimNow 配置参数（注册地址：[http://www.simnow.com.cn](http://www.simnow.com.cn)）：

| 参数 | 值 |
|------|----|
| BrokerID | 9999 |
| 行情服务器 | 180.168.146.187:10131 |
| 交易服务器 | 180.168.146.187:10130 |
| 产品名称 | simnow_client_test |
| 授权码 | 0000000000000000 |

---

## 第三部分：OpenBB 数据平台（可选）

统一接入多数据源，适合做数据研究和 AI 信号：

```bash
pip install openbb openbb-akshare
```

```python
from openbb import obb

# 港股行情
print(obb.equity.price.quote("0700.HK"))

# A 股历史数据
df = obb.equity.price.historical(
    symbol="600519",
    start_date="2024-01-01",
    provider="akshare"
).to_dataframe()

# 期货数据（直接用 akshare）
import akshare as ak
df = ak.futures_main_sina(symbol="RB0")
```

---

## 平台能力对比

| 功能 | macOS | Windows | Linux VPS |
|------|-------|---------|-----------|
| AKShare 数据 | ✅ | ✅ | ✅ |
| Backtrader 回测 | ✅ | ✅ | ✅ |
| VeighNa 回测 | ✅ | ✅ | ✅ |
| Futu 港股实盘 | ✅ | ✅ | ✅ |
| CTP 期货实盘 | ❌ | ✅ | ✅ |
| VeighNa Studio GUI | ❌ | ✅ | ❌ |

---

## 项目目录结构

```text
~/quant/
├── strategy/
│   ├── ma_futures.py       # 期货均线策略
│   └── ma_hk.py            # 港股均线策略
├── backtest/
│   ├── futures_backtest.py # 期货回测
│   └── hk_backtest.py      # 港股回测
├── live/
│   └── futu_trader.py      # 港股实盘
├── data/                   # 数据缓存
└── logs/                   # 运行日志
```

---

## 常用命令速查

```bash
# 激活环境
source ~/vnpy_env/bin/activate

# 退出环境
deactivate

# 测试港股数据
python -c "import akshare as ak; print(ak.stock_hk_hist(symbol='00700', period='daily', start_date='20241201', end_date='20241231', adjust='qfq').tail())"

# 测试期货数据
python -c "import akshare as ak; print(ak.futures_main_sina(symbol='RB0').tail())"

# 更新所有包
pip install --upgrade vnpy vnpy_ctastrategy futu-api backtrader akshare

# 检查 FutuOpenD 是否运行
curl -s http://127.0.0.1:11111 || echo "FutuOpenD 未启动"
```

---

## 注意事项

1. 港股实盘前务必先用 `ft.TrdEnv.SIMULATE` 仿真跑至少 1 周
2. 期货有杠杆，建议从 1 手开始，严格控制仓位
3. AKShare 数据适合日线级别，分钟线历史较短
4. Tushare 基本面数据更全，需注册获取 token：[https://tushare.pro/register](https://tushare.pro/register)
5. CTP 期货实盘只能在 Linux/Windows 上运行，macOS 仅做开发回测
